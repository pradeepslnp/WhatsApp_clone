class ChatModel {
  String name;
  String icon;
  bool isGroup;
  String time;
  String currentMessages;
  ChatModel(
      {this.name, this.icon, this.isGroup, this.time, this.currentMessages});
}
